export const goToHome = (history) => {
  history.push('/');
};

export const goToSejaSapiente = (history) => {
  history.push('/seja-sapiente');
};

export const goToVoluntario = (history) => {
  history.push('/voluntario');
};
